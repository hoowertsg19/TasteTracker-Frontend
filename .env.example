# Copia este archivo como .env y ajusta valores a tu entorno local
# Vite expone variables prefijadas con VITE_

# URL base de la API Laravel (incluye /api o /api/v1 según tu backend)
VITE_API_URL=http://localhost:8000/api

# Nombre de la app (sólo informativo)
VITE_APP_NAME=TasteTracker

# Modo actual (local, dev, prod) - libre según tu flujo
VITE_MODE=local
